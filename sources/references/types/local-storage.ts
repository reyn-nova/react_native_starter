// README: All these type's members below are just for example, remove all the members and create your own, then remove this comment after it

export type localStorageType = {
  SAMPLE_RESPONSE_JSON_KEY?: {
    api_status: number,
    api_message: string
  },
  SAMPLE_GENDER_KEY?: 'Man' | 'Woman',
  SAMPLE_BOOLEAN_KEY?: boolean
}
