
/**
  README: All these type's members below are just for example, 
  remove all the members and create your own, then remove this comment after it.
 */

import { User } from './user'

export type LocalStorage = {
  USER_KEY: User
  MESSAGING_TOKEN_KEY: string
}
